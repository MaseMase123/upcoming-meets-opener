const config = {
  checkInterval: 5000, // 5 seconds in milliseconds
  meetingBufferMinutes: 10, // Minutes before meeting to open
  calendarApiUrl:
    "https://www.googleapis.com/calendar/v3/calendars/primary/events",
};

export default config;
